using namespace B3D;
using namespace CB;

import math;

void Hook_FillRoom(CB::Room@ r) {
    float rand = random();
    if (r.Template.Name == "kce_lcz_storagebasement") {
        // == FILL LEVEL 2 ARCHIVE ==
        // == FILL LEVEL 3 ARCHIVE ==
        // == FILL LEVEL 4 ARCHIVE ==
    }
}